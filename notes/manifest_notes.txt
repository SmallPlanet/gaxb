TODO:
* Handle subelements
	- singular subelements
	- plural subelements
* ???




gaxb objc path_to_xml_schema -output path_to_folder

manifast.rule

link schema parts to template files

------------------------------------
Yet Another Formatted Document to Learn
Less glue code

<element "*" />  -> element.template
<element "Entity" />  -> entity.template

------------------------------------
Maximum customization
Extra work?

-- current working directory set by command line arguments

link("element", ".template")
run();


for all schema.elements
	if element is entity
		run_template("entity.h.template",
					  element,
					  "entity/Namespace_"..element.name".h");
  		run_template("entity.m.template",
  					  element,
  					  "entity/Namespace_"..element.name".m");
					  
	end if
end for

run_template("xml_loader.template", nil);
run_template("sqlite_loader.template", nil);
run_template("coredata_loader.template", nil);
run_template("foo_loader.template", nil);